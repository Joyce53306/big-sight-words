<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Big Class Sight Words</title>
    <link rel="apple-touch-icon" href="https://cdn-icons-png.flaticon.com/512/3406/3406828.png">
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
    <style>
        :root { --bg-color: #f0f7f4; --pink: #ffc2d1; --blue: #bde0fe; --green: #58cc02; }
        body { font-family: 'Arial Rounded MT Bold', sans-serif; background-color: var(--bg-color); margin: 0; padding: 20px; display: flex; flex-direction: column; align-items: center; min-height: 100vh; box-sizing: border-box; }
        #start-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(255,255,255,0.95); display: flex; flex-direction: column; align-items: center; justify-content: center; z-index: 100; }
        #start-btn { padding: 20px 50px; font-size: 30px; border-radius: 50px; background: #ffda08; border: none; cursor: pointer; font-weight: bold; }
        #header { font-size: 26px; font-weight: bold; color: #444; }
        .star-container { font-size: 35px; height: 50px; margin: 10px 0; color: #ffcc00; text-shadow: 1px 1px #888; }
        .button-container { display: flex; justify-content: center; gap: 20px; width: 100%; margin: 20px 0; }
        .word-btn { width: 150px; height: 150px; border-radius: 50%; border: 6px solid white; font-size: 26px; font-weight: bold; box-shadow: 0 8px 15px rgba(0,0,0,0.1); display: flex; align-items: center; justify-content: center; color: #333; }
        .btn-pink { background-color: var(--pink); }
        .btn-blue { background-color: var(--blue); }
        .correct-anim { transform: scale(1.1); border-color: var(--green); border-width: 8px; }
        #controls { display: flex; flex-direction: column; align-items: center; gap: 15px; width: 100%; }
        .action-btn { padding: 15px 30px; border-radius: 50px; border: 2px solid #ddd; background-color: #fff; font-size: 18px; font-weight: bold; width: 85%; max-width: 320px; }
        .record-btn { background-color: #ffd6a5; }
        .recording { background-color: #ff6b6b !important; color: white; }
        .play-btn { background-color: #c1fba4; }
        #next-btn { background-color: var(--green); color: white; font-size: 24px; border: none; display: none; }
    </style>
</head>
<body>

    <div id="start-overlay">
        <h1>Big Class Sight Words</h1>
        <button id="start-btn" onclick="startGame()">點我開始玩！</button>
    </div>

    <div id="header">找到正確的單字！</div>
    <div class="star-container" id="star-display"></div>

    <div class="button-container">
        <button id="btn-left" class="word-btn btn-pink" onclick="checkAnswer(0)">...</button>
        <button id="btn-right" class="word-btn btn-blue" onclick="checkAnswer(1)">...</button>
    </div>

    <div id="controls">
        <button class="action-btn" onclick="playTargetWord()">🔈 再聽一次老師唸</button>
        <button id="record-btn" class="action-btn record-btn" onclick="toggleRecording()">🎤 錄音練習</button>
        <button id="play-record-btn" class="action-btn play-btn" onclick="playRecording()" style="display:none;">▶️ 聽我的聲音</button>
        <button id="next-btn" class="action-btn" onclick="initGame()">🌟 答對了！下一題</button>
    </div>

    <script>
        const wordList = ["would", "I", "be", "No", "got", "tried", "can't", "ball", "one", "with", "which", "better", "may", "have", "first", "please", "What", "today", "need", "does", "a", "look", "see", "while", "David", "did", "draw", "his", "will", "not", "know", "That's", "for", "me", "put", "where", "if", "grow", "make"];
        let targetWord = "", currentOptions = [], stars = 0, mediaRecorder, audioChunks = [], canClick = true;

        function startGame() { document.getElementById('start-overlay').style.display = 'none'; initGame(); }

        function initGame() {
            canClick = true;
            document.getElementById('next-btn').style.display = 'none';
            document.getElementById('play-record-btn').style.display = 'none';
            document.getElementById('record-btn').innerText = "🎤 錄音練習";
            document.querySelectorAll('.word-btn').forEach(b => b.classList.remove('correct-anim'));
            const shuffled = [...wordList].sort(() => 0.5 - Math.random());
            currentOptions = [shuffled[0], shuffled[1]];
            targetWord = currentOptions[Math.floor(Math.random() * 2)];
            document.getElementById('btn-left').innerText = currentOptions[0];
            document.getElementById('btn-right').innerText = currentOptions[1];
            updateStars();
            setTimeout(playTargetWord, 500);
        }

        function playTargetWord() {
            window.speechSynthesis.cancel(); 
            const u = new SpeechSynthesisUtterance(targetWord);
            u.lang = 'en-US'; u.rate = 0.75; window.speechSynthesis.speak(u);
        }

        function playVictorySound() {
            const u = new SpeechSynthesisUtterance("Great job! You got five stars!");
            u.lang = 'en-US'; u.pitch = 1.5; u.rate = 1.2; window.speechSynthesis.speak(u);
        }

        function checkAnswer(index) {
            if (!canClick) return;
            if (currentOptions[index] === targetWord) {
                canClick = false;
                stars++;
                updateStars();
                
                if (stars >= 5) {
                    confetti({ particleCount: 300, spread: 100, origin: { y: 0.5 } });
                    playVictorySound();
                    setTimeout(() => {
                        alert("🎉 太棒了！集滿五顆星星囉！");
                        stars = 0; // 重置星星
                        initGame();
                    }, 500);
                } else {
                    confetti({ particleCount: 150, spread: 80, origin: { y: 0.6 } });
                    const correctBtn = index === 0 ? 'btn-left' : 'btn-right';
                    document.getElementById(correctBtn).classList.add('correct-anim');
                    document.getElementById('next-btn').style.display = 'block';
                }
            } else {
                alert("再聽一次喔！是 " + targetWord);
                playTargetWord();
            }
        }

        function updateStars() {
            const display = document.getElementById('star-display');
            display.innerText = stars === 0 ? "Ready!" : "⭐".repeat(stars);
        }

        async function toggleRecording() {
            const btn = document.getElementById('record-btn');
            if (mediaRecorder && mediaRecorder.state === "recording") {
                mediaRecorder.stop();
                btn.classList.remove('recording');
                btn.innerText = "✅ 錄好了";
            } else {
                try {
                    const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
                    mediaRecorder = new MediaRecorder(stream);
                    audioChunks = [];
                    mediaRecorder.ondataavailable = e => audioChunks.push(e.data);
                    mediaRecorder.onstop = () => {
                        const blob = new Blob(audioChunks, { type: 'audio/wav' });
                        window.recordedAudio = new Audio(URL.createObjectURL(blob));
                        document.getElementById('play-record-btn').style.display = 'block';
                    };
                    mediaRecorder.start();
                    btn.classList.add('recording');
                    btn.innerText = "正在錄音...";
                } catch (err) { alert("請開啟麥克風權限"); }
            }
        }

        function playRecording() { if (window.recordedAudio) window.recordedAudio.play(); }
    </script>
</body>
</html>
